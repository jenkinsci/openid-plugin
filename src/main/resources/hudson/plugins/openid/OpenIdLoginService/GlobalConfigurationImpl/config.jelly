<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
  <j:if test="${!instance.hidden}">
    <f:section title="${%OpenID Federated Login}">
    <f:entry field="enabled" >
      <f:checkbox default="false" title="${%Enable OpenID federated login service}"/>
    </f:entry>
    </f:section>
  </j:if>
</j:jelly>