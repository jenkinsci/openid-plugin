3. The way Hudson roundtrips OpenIDs currently associated with an user is insecure

4. Finish the SSO security realm

5. OpenID SSO realm needs to support user registration

6. With SSO realm, it shall not be possible to associate/delete OpenIDs with the user

7. Define an extension point to read group information. Implement the team extension support

8. Define a permission for registration so that only a subset of users can use Hudson in #5.

9. more testing, doc
